        // Simulación de login (sin conexión a base de datos)
        document.getElementById('loginForm').addEventListener('submit', function (e) {
            e.preventDefault();
            window.location.href = 'home.html'; // Redirección ficticia
        });
